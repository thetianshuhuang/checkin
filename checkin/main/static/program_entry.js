
class ProgramEntry {
	constructor(api_token, access_token) {
		this.api_token = api_token;
		this.access_token = access_token;
	}

	createEntry(container) {
		var c = document.getElementById(container);

		var row = document.createElement("div");
	}
}